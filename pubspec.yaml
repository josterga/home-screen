name: browser_home_page
description: A sleek browser home page with essential widgets.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  intl: ^0.18.1
  flutter_randomcolor: ^1.0.16
  weather: ^3.1.0
  google_fonts: ^5.1.0
  http: ^1.2.1
  html: ^0.15.2
  cupertino_icons: ^1.0.5

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
